import { ContactHero } from "@/components/contact/contact-hero";

export const metadata = {
  title: "Contact | tekjoe",
  description: "Get in touch — let's build something great together.",
};

export default function ContactPage() {
  return <ContactHero />;
}
